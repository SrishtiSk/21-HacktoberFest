#Python program accepting base and of the height of Triangle from the user and compute the area.
base= float(input("Enter the base length of triangle: "))
height=float(input("Enter the height: "))
print("Area of the Triangle with base "+str(base)+" and height "+str(height)+" is: "+ str(float((height*base)/2))+ "sq units")
